# Instagram profile screenshots

Drop your 5 Instagram profile screenshots into THIS folder using these EXACT
filenames. The moment a file is here, it shows up automatically inside the phone
mockup on the site (no code changes needed). Refresh the page after adding them.

| Save this screenshot | as this filename            |
|----------------------|-----------------------------|
| stics.ai profile     | `stics-ai.jpg`              |
| hollyfield.ia profile| `hollyfield-ia.jpg`         |
| stics.ia profile     | `stics-ia.jpg`              |
| stics.bot profile    | `stics-bot.jpg`             |
| stics.qc profile     | `stics-qc.jpg`              |

Notes
- Use the full portrait screenshots (the ones you already have). They are shown
  top-aligned inside the phone, so the header + first rows of posts are visible.
- `.jpg` is expected. If yours are `.png`, either rename them to `.jpg`
  (renaming the extension is fine) or tell me and I'll switch the code to `.png`.
- If a file is missing, the phone falls back to the recreated caption layout, so
  the site never looks broken.
